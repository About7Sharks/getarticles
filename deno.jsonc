{
  "tasks": {
    "gen-docs":"deno doc mod.ts --json >> denoDoc.json",
    "lint": "deno lint --unstable mod.ts",
    "test": "deno test --allow-net mod_test.ts",
    "build":"deno run -A _build.ts",
    "publish": "cd npm && npm publish --access public"
  }
}
